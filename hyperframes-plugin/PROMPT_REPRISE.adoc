= PROMPT_REPRISE — HyperFrames Gradle (Session 001 → 002)

_Session_ : 001
_Date_ : 2026-05-31
_Status_ : ✅ TERMINÉ — HF-1 Plugin Gradle stub + AsciidoctorJ intégration

'''
== État des lieux

Plugin Gradle fonctionnel. Structure build complète (wrapper 9.5.0,
version catalog, java-gradle-plugin, signing, publishing).
3 tâches Gradle enregistrées : generateHyperframesHtml, renderHyperframes.
3 tests GradleTestKit PASS. Build compile.

== Fait en session 001

- Structure build root + hyperframes-plugin/ (settings/build + wrapper)
- Version catalog hyperframes-plugin/gradle/libs.versions.toml
- HyperframesPlugin.kt, HyperframesExtension.kt
- GenerateHyperframesHtmlTask (AsciiDoc → HTML via AsciidoctorJ 3.0.0)
- RenderHyperframesTask (stub HF-1)
- 3 tests PASS (tasks listing, generation HTML, render fail sans generation)

== Reste à faire (session 002)

=== HF-2 — Bridge CLI HyperFrames (via plugin Gradle Node)

1. Ajouter `com.github.node-gradle:gradle-node-plugin` dans le version catalog
   (même pattern que slider-gradle : `libs.node.gradle`)
2. Configurer le plugin dans `RenderHyperframesTask` :
   - Node.js version managée par le plugin (pinnée)
   - Package npm `hyperframes` installé comme dépendance Gradle
   - ProcessBuilder vers le `node`/`npm` du plugin Gradle Node, pas le système
3. Implémentation de `RenderHyperframesTask` :
   - Résoudre le chemin `node` depuis `NodeExtension`
   - ProcessBuilder → `node /path/to/hyperframes/cli render ...`
   - Vérification exit code
   - Capture stderr
   - Timeout configurable (`extension.renderTimeoutMs`)
4. Gestion des erreurs :
   - Node.js pas encore download → message clair
   - Package hyperframes pas installé → message clair
5. Tests fonctionnels :
   - Mock CLI (script shell qui simule hyperframes)
   - Golden file MP4
6. Ajouter `extension.renderTimeoutMs` et `extension.nodeVersion` dans HyperframesExtension

== Commandes utiles

```
./gradlew :hyperframes:compileKotlin
./gradlew :hyperframes:test
./gradlew generateHyperframesHtml
```
