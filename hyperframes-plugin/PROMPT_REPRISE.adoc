= PROMPT_REPRISE — HyperFrames Gradle (Session 012 TERMINÉE → 013)
:session: 012
:date: 2026-07-09
:status: ✅ TERMINÉ — HF-7 évolution : câblage PronunciationDomainLoader dans le pipeline Gradle

'''
== État des lieux

Plugin Gradle fonctionnel avec pipeline complet :
AsciiDoc → AsciidoctorJ → HTML enrichi (stage, data-*, GSAP) → CLI Node.js → MP4.
EPIC HF-5 **TERMINÉ** : 4/4 templates livrés (title-card, code-diff, data-chart, kinetic-captions).
EPIC HF-6 **partiellement terminé** : CI ✅, Maven Central ✅, Gradle Portal à faire.
EPIC HF-7 **TERMINÉ + câblé** : dictionnaires pré-câblés `video-fr`/`video-en` +
câblage `pronunciationDomain.set("video-fr")` dans l'extension Gradle + merge
automatique auteur/domaine (author > domain > empty).
Version `0.0.1` publiée sur Maven Central via nmcp.

== Fait en session 012

- `HyperframesExtension.pronunciationDomain: Property<String>` (`@Input @Optional`)
- `GenerateHyperframesHtmlTask.pronunciationDomain` + chargement `PronunciationDomainLoader.load(it)`
- `HyperframesPlugin` wiring extension → task
- `HyperframesHtmlProcessor` refactor : constructeur internal avec `domainDictionary`,
  `withDomainDictionary()`, merge auteur (block + inline) → domaine en une seule passe,
  `mergeAuthorOverDomain` (hint auteur sans langue override toutes langues du domaine)
- 9 tests unit `HyperframesDomainWiringTest` (merge, override, baseline, inline, immutabilité)
- 3 tests fonctionnels `HyperframesPluginTest` (baseline, override, backward compat)
- 4 scénarios Cucumber `pronunciation_domain.feature` (full pipeline BDD EN)
- 1 test existant ajusté (empty block → no island, comportement plus cohérent)
- `./gradlew check` ✅ vert — 178 unit/functional + 33 Cucumber = 211 tests

== Prochaine session (013)

Priorité restante :
1. HF-6c — Publication Gradle Plugin Portal (handle cccp-education)
   Tâche de config/publish, pas de code. Dernier item pour clôturer EPIC HF-6.

== Routes documents clés (lecture rapide)

|===
| Document | Chemin | Utilité
| Architecture détaillée | `doc/HYPERFRAMES_ARCHITECTURE.adoc` | Pipeline global, décisions tech, contrat N3
| Pilotage agile | `doc/PILOTAGE_HYPERFRAMES.adoc` | Product vision, personas, US, story map, backlog
| Backlog | `BACKLOG.adoc` | User stories HF-1→HF-7, EPIC HF-7 détaillé, critères acceptation
| Règles absolues | `AGENT.adoc` | 9 règles (commits, publish, DAG, secrets, bridge, bounded context)
| Roadmap | `.agents/INDEX.adoc` | Statut EPICs HF-0→HF-7, sessions
|===

== Rappels techniques

- **Version publiée** : 0.0.1 (Maven Central, asciidoctorj 3.0.0 dans POM — local 3.0.1)
- **Pattern template** : value object DDD pur + `expand{Template}Blocks` processor
- **Template = data class** : `render(): String`, zéro effet de bord
- **Dictionnaires domaine** : `video-fr` / `video-en` (génériques vidéo, pas métier privé)
- **Pronunciation domain** : `hyperframes { pronunciationDomain.set("video-fr") }` active le merge
- **Merge precedence** : author (block > inline) > domain > empty. Author sans langue override toutes langues du domaine.
- **Cucumber** : `./gradlew cucumberTest` (tag filter `not @wip and not @integration`), Gherkin EN
- **CI** : `.github/workflows/test-hyperframes.yml` (JDK 25, 4 steps)
- **English only** : code, identifiants, commentaires, Gherkin — pas de français (règle AGENTS.adoc)
- **ZERO commit/push** sans permission explicite