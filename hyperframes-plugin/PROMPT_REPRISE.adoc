= PROMPT_REPRISE — HyperFrames Gradle (Session 003 → 004)

_Session_ : 003
_Date_ : 2026-05-31
_Status_ : ✅ TERMINÉ — HF-3 DSL AsciiDoc (blocs customs, data-* attributes, GSAP)

'''
== État des lieux

Plugin Gradle fonctionnel avec pipeline complet :
AsciiDoc → AsciidoctorJ → HTML enrichi (stage, data-*, GSAP) → CLI Node.js → MP4.
HyperframesHtmlProcessor post-processe le HTML pour les data-* attributes.
22 tests PASS. Build compile.

== Fait en session 003

- HyperframesHtmlProcessor : stage div, data-composition-id, data-track-*, animations GSAP
- Intégration dans GenerateHyperframesHtmlTask (post-processing après AsciidoctorJ)
- 10 tests unitaires processor + 5 tests intégration DSL
- Syntaxe AsciiDoc : `[.hyperframes-composition#intro]`, `[.hyperframes-track]`, `[.hyperframes-animation]`

== Reste à faire (session 004)

=== HF-4 — Intégration N3 runner-gradle

1. `collectHyperframesRetrieve` task → metadata.json
2. Contrat runner-gradle (HyperframesParseContext, JSON schema)
3. Tests intégration N3 (runner-gradle consume le JSON)

== Routes documents clés (lecture rapide)

|===
| Document | Chemin | Utilité
| Architecture détaillée | `doc/HYPERFRAMES_ARCHITECTURE.adoc` | Pipeline global, décisions tech, contrat N3
| Pilotage agile | `doc/PILOTAGE_HYPERFRAMES.adoc` | Product vision, personas, US, story map, backlog
| Backlog | `BACKLOG.adoc` | User stories HF-1→HF-6, critères acceptation, risques
| Règles absolues | `AGENT.adoc` | 8 règles (commits, publish, DAG, secrets, bridge)
| Roadmap | `.agents/INDEX.adoc` | Statut EPICs HF-0→HF-6, sessions
| Session courante | `.agents/sessions/NNN-sujet.adoc` | Détail de chaque session
|===

== Rappels techniques

- **Syntaxe AsciiDoc role+id** : utiliser `[.role#id]` PAS `[role, id="x"]` (heading id sur h2 enfant)
- **Bridge Node.js** : ProcessBuilder, pas de couplage npm dans le plugin
- **HyperframesHtmlProcessor** : pipeline 5 étapes (GSAP→stage→composition→track→animation)
- **Contrat N3** : voir `doc/HYPERFRAMES_ARCHITECTURE.adoc` section "Contrat N3"
- **ZERO commit/push** sans permission explicite

== Commandes utiles

```
./gradlew :hyperframes:compileKotlin
./gradlew :hyperframes:test
./gradlew generateHyperframesHtml
./gradlew renderHyperframes
```
